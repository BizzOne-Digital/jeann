export default async function InvitePage({ params }: { params: Promise<{ token: string }> }) { const { token } = await params; return <><h1 className="display text-3xl text-[var(--navy)]">Accept your invitation</h1><p className="mt-3 text-[var(--stone)]">This supplier or buyer portal invitation will be validated when you continue.</p><p className="mt-4 font-mono text-xs text-[var(--stone)]">Invitation: {token.slice(0, 8)}…</p><button className="btn btn-primary mt-6">Continue to account setup</button></>; }
